# General options
set-option -g history-limit 1024
set-option -g base-index 1
set-option -g mouse on
set-option -g default-terminal "tmux-256color"
set-option -g default-command "${SHELL}"

# ----------------------
# Bindings
# ----------------------
unbind C-b
# Remap prefix to (Control+a)
set-option -g prefix C-a
bind C-a send-prefix
# Reload tmux.conf with prefix-r
bind r source-file ~/.tmux.conf
# Secondary binding for C-l to retain redraw
bind C-l send-keys 'C-l'
# Easy rotating of panes
bind C-j swap-pane -U
# Bash backward compatibility
bind C-k send-keys 'C-k'
bind C-h send-keys 'C-h'

# ----------------------
# Vim compatible
# ----------------------
set-option -g status-keys vi
set-window-option -g mode-keys vi
bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T cope-mode-vi 'y' send -X copy-selection-and-cancel

# ----------------------
# Vim splits
# ----------------------
# Smart pane switching with awareness of VIM splits
#bind -n C-h run "(tmux display-message -p '#{pane_current_command}' | grep -iq vim && tmux send-keys C-h) || tmux select-pane -L"
#bind -n C-j run "(tmux display-message -p '#{pane_current_command}' | grep -iq vim && tmux send-keys C-j) || tmux select-pane -D" bind -n C-k run "(tmux display-message -p '#{pane_current_command}' | grep -iq vim && tmux send-keys C-k) || tmux select-pane -U"
#bind -n C-l run "(tmux display-message -p '#{pane_current_command}' | grep -iq vim && tmux send-keys C-l) || tmux select-pane -R"
#bind -n C-\ run "(tmux display-message -p '#{pane_current_command}' | grep -iq vim && tmux send-keys 'C-\\') || tmux select-pane -l"

# ----------------------
# Status Bar
# ----------------------
set-option -g status on
set-option -g status-position top
# Set colors for status bar and windows
set-option -g status-bg terminal
set-window-option -g window-status-current-style 'bg=black fg=terminal bold'
# Show hostname, session and window numbers
set-option -g status-left ''
set-option -g status-right '#H #[fg=terminal]#S:#I'
# Length of both sides in status bar
set-option -g status-left-length 20
set-option -g status-right-length 60
